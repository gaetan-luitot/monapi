// Duplicate this file and rename it 'config.js'

const dbConfig = {
    host: 'localhost',
    port: 3306,
    user: 'user',
    password: 'password',
    database : 'db_name',
};

module.exports = {
  dbConfig: dbConfig,
};
